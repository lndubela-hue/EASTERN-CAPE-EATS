3. **Deploy to Streamlit Community Cloud**:
- Go to [share.streamlit.io](https://share.streamlit.io/) and sign in with GitHub.
- Select this repository and choose `streamlit_app.py` as the main file.
- Deploy the app (takes ~1-2 minutes).
- Access the app via the provided URL.

4. **Manage via GitHub iOS App**:
- Open the GitHub iOS app.
- Navigate to this repository, edit `streamlit_app.py` or `requirements.txt`, and commit changes.
- Streamlit auto-updates the app on new commits.

### Local Development (Optional)
To run locally:
1. Install Python 3.8+.
2. Install dependencies:
```bash
pip install -r requirements.txt